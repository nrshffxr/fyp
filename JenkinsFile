pipelline {
  agent{ label 'linux' }
  options {
    buildDiscarder{logRotator(numToKeepStr: '5')
    }
    parameters {
      string(name: 'APP_NAME', defaultValue: '', description:'what is the heroku app name?')
    }
    stages 
    #incomplete_I_do_this_later
